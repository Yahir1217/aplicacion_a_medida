import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.micomercio.app',
  appName: 'MiComercio',
  webDir: 'dist/frontend/browser',
};

export default config;
